spring:
  application:
      name: admin-server
  cloud:
      config:
          uri: http://localhost:8888

eureka:
  instance:
      hostname: localhost
      prefer-ip-address: false
  client:
    service-url: http://localhost:8761/eureka/


server:
  port: 8050


